
<ui:ContentDialog
   x:Class="YingCaiAiWin.Views.Pages.AddRoleDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
     xmlns:local="clr-namespace:YingCaiAiWin.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Terms of use"
    d:DesignHeight="1000"
    d:DesignWidth="750"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    CloseButtonText="Close"
    DialogMaxWidth="750"
    mc:Ignorable="d">

    <ui:ContentDialog.Resources>
        <Style BasedOn="{StaticResource {x:Type ui:ContentDialog}}" TargetType="{x:Type local:AddRoleDialog}" />
    </ui:ContentDialog.Resources>

    <Grid  Width="380" Height="170">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 用户名 -->
        <TextBlock Grid.Row="0" Grid.Column="0" FontFamily="block"  FontWeight="Black"   VerticalAlignment="Center" Text="角色名称:" />
        <TextBox Grid.Row="0" Grid.Column="1" Height="40" Margin="0,4" x:Name="RoleName" />


        <TextBlock Grid.Row="1" Grid.Column="0" FontFamily="block"  FontWeight="Black"   VerticalAlignment="Center" Text="描述备注:" />
        <TextBox Grid.Row="1" Grid.Column="1" Height="40" Margin="0,4" x:Name="RoleDes" />
        
        <ui:InfoBar Title="错误"   Grid.Row="2" Grid.Column="1" Height="80" x:Name="ErrorInfoBar"  Message="只能输入数字和英文！" IsOpen="False"  Severity="Error"  />

    </Grid>


</ui:ContentDialog>

