
<ui:ContentDialog
   x:Class="YingCaiAiWin.Views.Pages.AddSystemConfig"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
     xmlns:local="clr-namespace:YingCaiAiWin.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Terms of use"
    d:DesignHeight="600"
    d:DesignWidth="750"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    CloseButtonText="Close"
    DialogMaxWidth="750"
    mc:Ignorable="d">

    <ui:ContentDialog.Resources>
        <Style BasedOn="{StaticResource {x:Type ui:ContentDialog}}" TargetType="{x:Type local:AddSystemConfig}" />
    </ui:ContentDialog.Resources>

    <Grid Margin="10" Width="500" Height="450">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 用户名 -->
        <TextBlock Grid.Row="0" Grid.Column="0" FontFamily="block"  FontWeight="Black"   VerticalAlignment="Center" Text="标题:" />
        <TextBox Grid.Row="0" Grid.Column="1" Height="40" Margin="0,4" x:Name="Filename"   />

        <TextBlock Grid.Row="1" Grid.Column="0" FontFamily="block"  FontWeight="Black"   VerticalAlignment="Center" Text="内容:" />
        <TextBox  Grid.Row="1" Grid.Column="1"
                x:Name="Content" 
                TextWrapping="Wrap"
                FontSize="14"
                  Padding="10"
                Foreground="Black"
                Height="300"
                Width="400"
                IsReadOnly="False"
                VerticalScrollBarVisibility="Auto"
                  HorizontalContentAlignment="Left"
                  VerticalContentAlignment="Top"
                  AcceptsReturn="True"
                 AcceptsTab="True"
            />
        <TextBlock Grid.Row="2" Grid.Column="0" FontFamily="Comic Sans MS" FontWeight="Black" VerticalAlignment="Center" Text="是否调用AI:" />
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
            <RadioButton x:Name="radio1_yes" Content="是" GroupName="GroupAI" Margin="0,0,10,0"/>
            <RadioButton x:Name="radio1_no" Content="否" IsChecked="True"  GroupName="GroupAI"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Grid.Column="0" FontFamily="Comic Sans MS" FontWeight="Black" VerticalAlignment="Center" Text="是否联网:" />
        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal">
            <RadioButton x:Name="radio2_yes" Content="是" GroupName="GroupNetwork" Margin="0,0,10,0"/>
            <RadioButton x:Name="radio2_no" Content="否" IsChecked="True" GroupName="GroupNetwork"/>
        </StackPanel>

        <ui:InfoBar Title="错误"   Grid.Row="4" Grid.Column="1" Height="80" x:Name="ErrorInfoBar"  Message="" IsOpen="False"  Severity="Error"  />
    </Grid>


</ui:ContentDialog>